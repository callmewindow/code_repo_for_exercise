// https://leetcode.cn/problems/lwyVBB/
function isAlienSorted(words: string[], order: string): boolean {
  // 经典搭建顺序题目
  // 对words直接顺序遍历即可，每次比较相邻的两个

  // 对于具体两个词的判断，需要根据各个字符在order中的顺序来进行
  // 为了尽快得到字符的顺序，搭建一个哈希表来进行

  // 进而只需基于字典序的知识判断比较即可
  
};